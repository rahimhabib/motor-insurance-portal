# Email Configuration Template
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to Git (it's already in .gitignore)

# Internal team email address (where lead notifications go)
TEAM_EMAIL=motor-team@insurancecompany.com

# Email service configuration
# For Gmail/Outlook with Nodemailer:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM=noreply@insurancecompany.com

# For SendGrid (if using SendGrid):
# SENDGRID_API_KEY=SG.your-sendgrid-api-key-here

# For AWS SES (if using AWS SES):
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY=your-aws-access-key
# AWS_SECRET_KEY=your-aws-secret-key

# For Resend (if using Resend):
# RESEND_API_KEY=re_your-resend-api-key-here

